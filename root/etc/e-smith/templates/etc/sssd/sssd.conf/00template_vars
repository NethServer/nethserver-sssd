{
    #
    # 00template_vars
    #
    
    use NethServer::SSSD;
    our $sssd_object = NethServer::SSSD->new();

    our $suffix = $sssd_object->baseDN();
    our $provider = $sssd{'Provider'} || 'ldap';
    return '';
}
